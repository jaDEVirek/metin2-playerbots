diff --git a/game/src/cmd_general.cpp b/game/src/cmd_general.cpp
--- a/game/src/cmd_general.cpp
+++ b/game/src/cmd_general.cpp
@@ -2357 +2357,7 @@
-		char country_code[3];
+		// Uninitialised on every locale the switch below does not name -
+		// including a live server, where the default branch only fills it
+		// on test_server. Whatever was on the stack went into the URL:
+		// "/ishop?pid=1&c=\x02&sid=0" and Apache answered 400 before PHP
+		// ever ran (archded, 10 September, Debian/VPS). The shop reads the
+		// code only as a language hint, so "en" is the right fallback.
+		char country_code[3] = "en";
