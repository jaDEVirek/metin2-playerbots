# Files copied into a server update ZIP. Keep .env and databases out.
# A line may use a wildcard in its last segment; it expands to what is there.
files/admin_panel.py
# The rest of what prepare-context.sh stages out of files/. That script does not
# run on a player's machine - start-server.ps1 stages these instead - so every
# one of them has to travel with the update or the copy in the image stays at
# whatever the installer shipped.
files/items.json
files/favicon.png
files/web_admin_schema.sql
files/web_admin.quest
# Seban's panel: the whole tree, icons included. It is built on the player's
# machine like the classic panel, so what ships is the source.
linux-port/docker/seban-panel/*
files/high_risk.quest
files/speed_boost.quest
# The panel's skill icons. Two copies for the same reason admin_panel.py has
# two: files/ is the source and linux-port/docker/panel/app/ is the build
# context the image is actually made from, and an update that refreshed only
# one of them would leave the running panel behind the sources.
# Only the source copy travels. start-server.ps1 stages it into
# panel/app/static on the way to the build, and the staged directory also holds
# 4700 generated item icons that are not in the source tree - shipping it whole
# would put seventeen megabytes of them into every update and overwrite an
# operator's own regenerated set.
files/static/*
linux-port/docker/docker-compose.yml
linux-port/docker/.env.example
linux-port/docker/mariadb/initdb.d/10-import-dumps.sh
linux-port/docker/mariadb/conf.d/99-metin2.cnf
# Waits for the database before seeding. Its timeout decides whether a slow
# recovery fails the whole compose "up".
linux-port/docker/mariadb/playerbot/apply.sh
linux-port/docker/mariadb/playerbot/itemshop_schema.sql
linux-port/docker/itemshop/*
# Discovers the overlay sources and copies them into the build context.
linux-port/docker/prepare-context.sh
linux-port/docker/panel/app/admin_panel.py
# The panel image entrypoint: it refreshes the database credentials in an
# existing m2panel.conf, so a panel-conf volume from an older install follows
# .env instead of answering "Access denied" while the game runs.
linux-port/docker/panel/bin/entrypoint.sh
linux-port/overlays/playerbot/serverfiles/mob_drop_item.m3.append.txt
linux-port/overlays/playerbot/serverfiles/special_item_group.moonlight.txt
linux-port/overlays/playerbot/sql/playerbots_seed.sql
# ...and the copy the migrate container actually mounts and runs. The launcher
# refreshes it before every start, but shipping it as well means an install that
# starts compose directly is not silently seeding an August-era registry.
linux-port/docker/mariadb/playerbot/playerbots_seed.sql
linux-port/overlays/playerbot/tools/generate_seed.py
# The nickname pool, and the two copies it lives in for the same reason
# the seed does: the overlay is the source, mariadb/playerbot is what the
# migrate container mounts, and an update that refreshed only one of them
# would run a pool nobody generated.
linux-port/overlays/playerbot/sql/playerbot_names.sql
linux-port/docker/mariadb/playerbot/playerbot_names.sql
linux-port/overlays/playerbot/tools/generate_bot_names.py
linux-port/overlays/playerbot/data/bot_names_community.txt
linux-port/overlays/playerbot/data/bot_names_iwakura.txt
# The overlay sources, and the staged build context they are compiled from.
# Wildcards, not lists: splitting a source file must not require editing this
# file, and forgetting to is what shipped a manager without its own headers in
# 1.22.4 and again in 1.23.2. The packager refuses a package in which the two
# sides disagree.
linux-port/overlays/playerbot/src/game/src/playerbot_*
linux-port/docker/game/src/server/game/src/playerbot_*
# Engine files carrying an overlay patch added after the full package was
# built. prepare-context.sh never runs on a player's machine, so a patch that
# only lives under overlays/playerbot/patches reaches nobody; the staged,
# patched file does - the same way the playerbot sources do.
linux-port/docker/game/src/server/game/src/char.cpp
linux-port/docker/game/src/server/game/src/char_battle.cpp
# Patch 0014: the channel status and the login limit count players, not
# bots (desc_manager, p2p, desc_client). Patch 0015: country_code in
# do_in_game_mall has a default (cmd_general). Staged like the rest.
linux-port/docker/game/src/server/game/src/desc_manager.cpp
linux-port/docker/game/src/server/game/src/desc_client.cpp
linux-port/docker/game/src/server/game/src/p2p.cpp
linux-port/docker/game/src/server/game/src/p2p.h
linux-port/docker/game/src/server/game/src/cmd_general.cpp
linux-port/docker/game/src/server/game/src/char_state.cpp
linux-port/docker/game/src/server/game/src/input_db.cpp
linux-port/docker/game/src/server/game/src/cmd.cpp
linux-port/docker/game/src/server/game/src/cmd_gm.cpp
linux-port/docker/game/src/server/game/src/item_manager.cpp
linux-port/docker/game/src/server/game/src/config.cpp
linux-port/docker/game/src/server/game/src/input_main.cpp
linux-port/docker/game/src/server/game/src/shop.h
# The engine patches, by pattern for the same reason as the sources above: a
# new one must not need an edit here to reach the people running the server.
# The launcher applies whatever it finds in that directory and decides for
# itself which of them the installed tree already has.
linux-port/overlays/playerbot/patches/*.patch
# Assigns maps to game cores. The bots' route now includes Orc Valley, and
# a server-side bot cannot cross between cores, so this must ship with it.
# Every container-side script, not just the one that assigns maps.
# Reported from the Discord: an install with no panel/bin/apply_rates.sh had
# no way to get one, because updates carried entrypoint.sh and nothing else
# beside it - and the same held for m2-rates, m2-supervise, m2-gm and m2-lang,
# which are the whole of how the game container is driven. A script that ships
# only in the installer is a script no update can ever repair.
linux-port/docker/game/bin/*
linux-port/docker/panel/bin/*
linux-port/docker/game/Dockerfile
# Every other build context that "docker compose up" builds on the player's
# machine. Only the game's Dockerfile used to ship, and an update therefore
# could not repair one of the others: a two-byte panel/Dockerfile - reported
# from the Discord - failed the bake before it started, so game and seban-panel
# were CANCELED with it and nothing came up at all. Clicking PLAY again cannot
# help, because nothing in the update replaces the broken file.
linux-port/docker/panel/Dockerfile
linux-port/docker/panel/.dockerignore
linux-port/docker/game/.dockerignore
linux-port/docker/client-builder/Dockerfile
linux-port/docker/client-builder/.dockerignore
linux-port/docker/webclient-fetcher/Dockerfile
linux-port/docker/wsbridge/Dockerfile
linux-port/docker/wsbridge/.dockerignore
linux-port/docker/updater/Dockerfile
# ...and everything those Dockerfiles COPY out of their own build context.
# A Dockerfile that ships without what it reads fails the build at the COPY
# instead of at the bake, which is what "failed to compute cache key ...
# /rates: not found" means, and no amount of clicking PLAY can repair it.
#
# Being tracked by Git is not the same as being delivered: an install put
# together from an update package has only what this file lists, and
# panel/bin/apply_rates.sh was tracked and still missing on the machine that
# reported it. Whatever a COPY names has to be here, or be written on the
# player's machine by start-server.ps1 - there is no third way in.
#
# rates/ and pack/ carry only their README from us: the pack.sh beside it is
# cut from the operator's own reference server files by prepare-context.sh and
# is not ours to hand out. The README is enough to make the directory exist,
# and both Dockerfiles already test for the script before using it.
linux-port/docker/game/rates/*
linux-port/docker/client-builder/pack/*
linux-port/docker/client-builder/bin/*
linux-port/docker/updater/bin/*
linux-port/docker/wsbridge/bin/*
# The chest table the Dockerfile COPYs, at the path the COPY reads it from.
# The update builds the image the moment the files land, before start-server
# gets to stage it, so the package puts it there itself.
linux-port/docker/game/special_item_group.moonlight.txt
linux-port/docker/game/mob_drop_item.m3.append.txt
PACZKA_INFO.txt
CHANGELOG.md
VERSION
start-server.ps1
start.bat
stop.bat
Metin2-Launcher.ps1
Metin2-Launcher.bat
Metin2-Launcher-GUI.ps1
Metin2-Launcher-GUI.bat
launcher/Metin2Launcher.psm1
launcher/Metin2Launcher.Diagnostics.psm1
launcher/launcher.config.example.json
launcher/update-manifest.example.json
launcher/server-update-files.txt
launcher/client-update-files.example.txt
docs/LAUNCHER.md
tools/New-M2UpdatePackage.ps1
